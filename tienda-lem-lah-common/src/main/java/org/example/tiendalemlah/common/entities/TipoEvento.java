package org.example.tiendalemlah.common.entities;

/**
 * Tipos de evento de seguridad que se auditan.
 * El enunciado obliga a usar un enumerado para esto.
 */
public enum TipoEvento {
    LOGIN_EXITOSO,
    LOGIN_FALLIDO,
    LOGOUT
}